version: '3.8'

services:
  rakta-infotech:
    build: .
    container_name: rakta_infotech_web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ./data:/var/www/html/data
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
